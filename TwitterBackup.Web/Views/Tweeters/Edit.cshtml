@model TwitterBackup.Web.Models.TweeterViewModels.EditTweeterViewModel


@{
    ViewData["Title"] = "Edit";
}

<h2>Edit</h2>

<h4>Tweeter</h4>
<hr />
<div class="row">
    <div>
        <form asp-action="Edit" id="edit-form">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>

            <input type="hidden" asp-for="TweeterId" />
            <input type="hidden" asp-for="UserName">
            <input type="hidden" asp-for="Description">

            <label asp-for="Description" class="control-label"></label>
            <p>@Html.DisplayFor(model => model.Description)</p>

            <div class="form-group">
                <label asp-for="TweeterComments" class="control-label"></label>
                <input asp-for="TweeterComments" class="form-control col-md-5" @*onkeypress="this.style.width = ((this.value.length + 1) * 8) + 'px';" *@ />
                <span asp-validation-for="TweeterComments" class="text-danger"></span>
            </div>
            <br />
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>
}